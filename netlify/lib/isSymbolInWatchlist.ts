

export const isSymbolInWatchlist = async(userId: string, symbol: string): Promise<boolean> => {
  // const items = await getAllWatchlistItems(userId);
  // return items.some((item) => item.symbol === symbol);

  return Promise.resolve(false);
}

// function getAllWatchlistItems(userId: string) {
//   throw new Error("Function not implemented.");
// }
